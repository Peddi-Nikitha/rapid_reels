# Client must only use publishable key. Never place Stripe secret keys in app code.
# Payment intents are created by backend (Firebase Functions).
#
# Optional: override publishable key at build time:
# flutter run --dart-define=STRIPE_PUBLISHABLE_KEY=pk_test_...
