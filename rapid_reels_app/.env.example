# Stripe keys live in lib/core/config/stripe_config.dart (publishable + secret).
# Payment intents are created from the app via https://api.stripe.com (no Cloud Functions).
#
# Optional: override publishable key at build time:
# flutter run --dart-define=STRIPE_PUBLISHABLE_KEY=pk_test_...
